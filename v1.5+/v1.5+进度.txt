目前进度：（此处*为已完成项，可在团队仓库关注更新动态，GitHub：https://github.com/lantiansb/Noise-monitor.git）

下位机：
*①nRF24L01（收发）库编写，系统接口编写；
*②下位机硬件设计，基于原来的版本进行修改，删除4G模块，更换为2.4G模块；
③接收终端（插入PC机）固件编写，用于与PC机进行简单通信和数据传入；
④接收终端硬件设计，由于串行口通信可能没有SPI快，考虑插入电脑便携硬件接口还是使用了USB口，可能会牺牲掉一点历史数据接收速率；

上位机：
*①界面设计，可以仿照小程序；
②PC机在环实时数据回传（1sa/s）功能设计；
③上位机点击获取历史数据，免得每次进入都获取导致下位机阻塞，更改为手动选择获取，即默认进入上位机时图表只从当前时间开始绘图，
若选择获取历史数据，则先命令下位机进行回传，成功后下位机正常回传实时数据，而上位机绘制历史数据后绘制实时数据。（历史数据处理有一定难度）